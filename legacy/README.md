# Archived legacy UI

`family-tree-radial-v2.html` is the old Babel-in-browser explorer. PeopleGraph now serves the Vite SPA from `frontend/dist`.

The `/legacy` route is retired (HTTP 410) and points families to `/`.
